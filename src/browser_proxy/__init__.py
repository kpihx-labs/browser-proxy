"""Provide the versioned browser-proxy package.

The package exposes a socket-activated Edge CDP daemon and its JSON-RPC CLI.

Examples:
    >>> __version__
    '0.2.0'
    >>> isinstance(__version__, str)
    True
"""

__version__ = "0.2.0"
